"""
backend.routers

One module per API surface area, mirroring the Orchestrator's
own pipeline split (decision / simulation / knowledge / general),
plus a unified /query endpoint and basic session/health utilities.
"""